language: python

python:
  - 2.7

build:
  pre_ci:
    - docker pull therickys93/ubuntu14
    - docker build --rm -t therickys93/ubuntu14node .

  pre_ci_boot:
    image_name: therickys93/ubuntu14node
    image_tag: latest
    pull: false

  ci:
    - bash /node/test/test.sh
